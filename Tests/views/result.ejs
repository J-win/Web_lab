<!DOCTYPE html>
<html>
<head>
<title>Результаты</title>
</head>
<body>
<% for (var i = data.length - 1; i >= 0; i--) { %>
<table>
    <% for (var j = 0; j < data[i].result.length; j++) { %>
      <tr>
        <td> <%= j + 1 %> </td>
        <td> <%= data[i].result[j] %> </td>
      </tr>
    <% } %>
</table>
<br>
<% } %>
</body>
</html>
